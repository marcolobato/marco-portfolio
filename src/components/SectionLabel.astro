---
// SectionLabel.astro — Small lemon pill used to introduce each major section.
// Usage: <SectionLabel label="Selected Work" />
// Styles live in src/styles/global.css — section 7. Section Label

const { label } = Astro.props;
---

<p class="section-label">{label}</p>
